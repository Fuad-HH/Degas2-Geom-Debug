All the generated files are included here. Just executing the notebooks should work.

Look at `create_original_mesh.ipynb` first and then `identify_dg2_mesh_cells.ipynb` should give the full picture. The approach in `reverseengineering-degas2-mesh-to-mtri.ipynb` did not work.

